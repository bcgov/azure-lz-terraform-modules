name: Deny-Resource-Types-Assignment
type: Microsoft.Authorization/policyAssignments
properties:
  displayName: "Enforce Denied Resource Types"
  description: "Assignment of the Deny-Resource-Types policy to restrict deployment of specific resource types."
  policyDefinitionId: "/providers/Microsoft.Authorization/policyDefinitions/Deny-Resource-Types"
  scope: "${current_scope_resource_id}" # The ALZ provider automatically resolves the correct resource ID for each management group. This is done based on which archetype the management group references, and what policy assignments are referenced in that archetype.
  parameters:
    effect:
      value: "Deny"
    deniedResourceTypes:
      value:
        - "Microsoft.ClassicCompute/virtualMachines"
        - "Microsoft.Sql/servers"
        # Add more resource types as needed
  metadata:
    assignedBy: "Azure Landing Zone"
    category: "General"
    version: "1.0.0"
